# Public (safe for browser)
NEXT_PUBLIC_SUPABASE_URL=https://fxashuozglfcevdivtud.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server-only. Never expose to the browser.
SUPABASE_SERVICE_ROLE_KEY=
# Optional: shared secret for provider webhooks / machine ingest (Authorization: Bearer <key>)
BENFILE_INGEST_API_KEY=
# Optional: enables the AI executive summary (server-side only). Leave empty to use the deterministic summary.
ANTHROPIC_API_KEY=
